services:
  - type: web
    name: broken-page-checker
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "streamlit run app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
